# ═══════════════════════════════════════════════════════════════════════════════
# Добавить в config.ini (все параметры опциональные)
# ═══════════════════════════════════════════════════════════════════════════════
 
[NETWORK]
# Fallback роль устройства, если группа Zabbix не определена
default_role = Network
 
# Fallback site, если тег "site" отсутствует на хосте
# default_site = default
 
# Fallback device type, если модель устройства не определена
# default_device_type = Generic Network Device
 